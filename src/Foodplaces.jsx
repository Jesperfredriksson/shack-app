import React, { Component } from "react";
import { render } from "react-dom";
class Foodplaces extends React.PureComponent {
  render() {
    RandomList = () => {
      f = [Math.floor(Math.random() * f.length)];
      return <div>{Randomlist}</div>;
    };
    const f = ["Phil's", "Bastards", "Pastastället", "Borgen", "Icasallad"];
    return;
  }
}

export default Foodplaces;
